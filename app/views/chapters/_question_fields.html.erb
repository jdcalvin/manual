<div class="nested-fields">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  	<%= link_to_remove_association 'Remove Question', f, data: { confirm: "Are you sure?" } %>
		<%= f.hidden_field :_destroy %>
</div>
